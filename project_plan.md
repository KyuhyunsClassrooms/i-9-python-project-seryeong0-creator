# 프로젝트 계획서

## 1. 프로젝트 주제

내가 만들 프로그램의 주제:
우리 학교 건물 피난 통로 구조 적합성 진단 프로그램

---

## 2. 문제 상황

이 프로그램이 해결하려는 문제를 설명하세요.

내 설명:
매일 생활하는 학교 건물의 복도가 쉬는 시간이나 이동 수업 때마다 매우 혼잡해지는 것을 관찰했다. 만약 화재나 지진 같은 실제 재난 상황이 발생했을 때, 현재 학교 건물의 구조가 많은 학생이 안전하고 신속하게 대피하기에 적절한지 건축학적 관점에서 검증하고 개선점을 찾아보고 싶다.

---

## 3. 입력-처리-출력 구조

### 입력

사용자가 입력하거나 프로그램이 사용할 데이터는 무엇인가?

- 진단하고자 하는 학교 구역 번호
- 해당 구역에 현재 상주하고 있는 대피 인원수 

### 처리

입력된 데이터를 어떤 규칙이나 알고리즘으로 처리할 것인가?

- 사용자가 선택한 구역 번호에 맞는 건축 설계 데이터(복도 너비, 문 개수, 계단 거리)를 2차원 리스트에서 가져온다.
- 대피 계단까지의 거리가 건축 법적 기준인 30m를 초과하는지 검사하여 감점한다.
- (대피 인원수 ÷ 문의 개수)를 계산하여 한 문에 20명이 넘는 인원이 몰리는지 계산하여 감점한다.
- 계산된 최종 점수를 기준으로 안전(80점 이상), 주의(60점 이상), 위험(60점 미만) 등급을 판정한다.

### 출력

프로그램이 최종적으로 무엇을 출력하는가?

- 선택한 학교 구역의 건축 설계 정보 리포트
- 계산된 구조 안전 점수 및 종합 적합성 등급 (안전/주의/위험)

---

## 4. 2차원 리스트 설계

내 프로그램에서 사용할 2차원 리스트의 이름:school_zones

school_zones = [
    ["3층 2학년 교실복도", 120, 2, 15],
    ["2층 본관-신관 연결통로", 100, 1, 35],
    ["1층 급식실 나가는 통로", 240, 3, 10],
    ["4층 동아리실 복도", 150, 1, 28]
]
### 한 행의 의미
학교 건물 내에서 진단 대상이 되는 하나의 특정 구역 공간 정보를 의미한다.

### 각 열의 의미

| 열 번호 | 의미 | 예시 |
|---:|---|---|
| 0번 열 | 구역 이름 (문자열) | 3층 2학년 교실복도 |
| 1번 열 | 3층 2학년 교실복도 |  120 |
| 2번 열 | 외부 탈출용 문의 개수 (정수, 단위: 개) | 2 |
| 3번 열 | 대피 계단까지의 실제 거리 (정수, 단위: m) | 15 |

---

## 5. 함수 설계

| 함수 이름 | 역할 | 입력값 | 반환값 또는 출력 |

| show_menu | 진단 가능한 학교 구역의 목록을 번호와 함께 화면에 출력한다. | zones (2차원 리스트) | 없음 (화면 출력만 수행) |
| check_safety |  구역 정보와 인원수를 받아 거리와 병목 현상을 계산하고 안전 등급을 매긴다.| 구역 정보와 인원수를 받아 거리와 병목 현상을 계산하고 안전 등급을 매긴다. | score (정수), grade (문자열) |
| print_report | 최종 안전 점수, 등급, 그리고 건축학적 개선 조언이 담긴 진단서를 출력한다. | zone (리스트), students (정수), score (정수), grade (문자열) | 없음 (화면 출력만 수행) |

---

## 6. 사용할 제어구조

### 조건문을 사용할 부분

내 계획:check_safety 함수 내에서 대피 거리가 30m를 초과하거나 문당 대피 인원이 20명을 초과하는지 비교 연산할 때 사용한다.

계산된 점수가 80점 이상, 60점 이상 등 구간별로 등급을 나눌 때 if-elif-else 구조를 사용한다.

메인 루프에서 사용자가 리스트에 없는 잘못된 번호를 입력했을 때 예외 처리를 하기 위해 사용한다.


### 반복문을 사용할 부분

내 계획:show_menu 함수에서 2차원 리스트에 저장된 전체 학교 구역 데이터를 하나씩 꺼내어 화면에 메뉴판 형태로 출력할 때 for 문을 사용한다.

사용자가 프로그램을 종료(0번 입력)하기 전까지 계속해서 다른 구역을 반복하여 진단할 수 있도록 메인 흐름에 while True 무한 루프를 사용한다.


---

## 7. 예상되는 예외 상황

1. 사용자가 제공된 구역 번호 목록(예: 1~4번) 외에 5번이나 -1번 같은 잘못된 번호를 입력하는 경우
2. 인원수 입력 란에 음수(-)나 문자를 입력하여 계산 오류가 발생할 수 있는 경우

---

## 8. Gemini에게 도움받고 싶은 부분

1. 건축학적 개념(피난 계획, 병목 위험 기준)을 고등학생 수준의 파이썬 조건문으로 모델링하는 방법
2. 2차원 리스트의 인덱스 구조를 다룰 때 헷갈리는 부분에 대한 오류 점검
3. 사용자가 잘못된 번호를 입력했을 때 프로그램이 튕기지 않게 방어하는 예외 처리 코드 작성 힌트
